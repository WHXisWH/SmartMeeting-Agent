"""
SmartMeet Custom Agent package (zero external pip dependencies during deploy).

Notes / 注意:
- Keep this package small and self-contained to avoid remote build failures.
- Do not import heavy libraries at module import time.
"""

